# Copy to .env and fill in values

SECRET_KEY=change-me-in-production

# CAS — SPbSTU single sign-on
CAS_SERVER=https://cas.spbstu.ru

# Full URL of the backend /api/auth/callback endpoint.
# Dev:  use ngrok → https://<your-id>.ngrok-free.app/api/auth/callback
# Prod: https://poly.hex8d.space/api/auth/callback
CAS_SERVICE_URL=https://<your-ngrok-id>.ngrok-free.app/api/auth/callback

# Where backend redirects the browser after successful login.
# Dev (vite dev server): http://localhost:5174
# Dev (Docker):          http://localhost:8080
# Prod:                  https://poly.hex8d.space
FRONTEND_URL=http://localhost:5174
